<%= form_for(local_issue) do |f| %>
  <% if local_issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(local_issue.errors.count, "error") %> prohibited this local_issue from being saved:</h2>

      <ul>
      <% local_issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :desription %>
    <%= f.text_field :desription %>
  </div>

  <div class="field">
    <%= f.label :priority_level_id %>
    <%= f.select :priority_level_id,
      [['Low', 1], ['Medium', 2], ['High', 3], ['Critical', 4]]
    %>
  </div>

  <div class="field">
    <%= f.label :fixer_id %>
    <%= f.select :fixer_id,
      [['Mariano Italiano', 139283]] %>
  </div>

  <div class="field">
    <%= f.label :tester_id %>
    <%= f.select :tester_id,
      [['Mariano Italiano', 139283]]  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
